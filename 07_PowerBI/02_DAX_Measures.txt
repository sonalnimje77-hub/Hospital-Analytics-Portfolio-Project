Total Patients = DISTINCTCOUNT(dim_patient[patient_id])
Total Appointments = COUNTROWS(fact_appointments)
Completed Appointments = CALCULATE([Total Appointments], fact_appointments[status]="Completed")
Total Revenue = SUM(fact_billing[payable_amount])
Average Bill Value = DIVIDE([Total Revenue], COUNTROWS(fact_billing))
Average Waiting Minutes = AVERAGE(fact_waiting_time[waiting_duration_minutes])
Average Patient Rating = AVERAGE(fact_patient_feedback[rating_overall])
Occupied Beds = CALCULATE(COUNTROWS(fact_bed_occupancy), fact_bed_occupancy[is_occupied]=TRUE())
Bed Occupancy % = DIVIDE([Occupied Beds], COUNTROWS(fact_bed_occupancy))
